<% for post in @posts %>
  <div class="content">
    <h1><%= link_to post.title, user_post_path(post.user, post) %></h1>
    <h2><%= post.lead %></h2>
    <p>Ecrit par <%= link_to post.user.fullname, user_path(post.user) %>, le <%= post.created %></p>
  </div>
  <hr />
<% end %>